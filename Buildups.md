**13Feb**
##Memory Layer
#1 CPU ALLOCATOR:
 ~Fetches Memory From CPU
  ~Aligned;Explicit Allocation(no copying,assignement)Free,zero,allocate.
#2 Global Memory Pool:
~Memory expands with demand
 ~Internal Memory Pool(single fixed size)
